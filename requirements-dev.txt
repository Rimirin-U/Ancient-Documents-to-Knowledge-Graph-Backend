# =============================================================================
# 开发 / 集成测试（在 requirements.txt 基础上追加）
# -----------------------------------------------------------------------------
#   pip install -r requirements-dev.txt
#
# 从仓库根目录：使用根目录 requirements-dev.txt
# =============================================================================

-r requirements.txt

pytest==9.0.2
requests==2.32.5
